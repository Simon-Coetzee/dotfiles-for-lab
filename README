# Config files for R and emacs
This is my directory of config files for R and emacs that I use for R devel

To use this repo, just link the files in it to your home directory

for example:
```shell
ln -s /full/path/to/dotfiles-for-lab/emacs ~/.emacs
```
